// Declarando variáveis com 'let'
let produto = "Chocolate em Barra";
let preco_unitario = 7.50;
let quantidade = 3;

// Processamento
let valor_total = preco_unitario * quantidade;

// Saída de dados no console
console.log(`Produto: ${produto}`);
console.log(`Valor Total: R$ ${valor_total}`);